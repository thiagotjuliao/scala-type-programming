// The formatting contract, enforced from the build: `sbt scalafmtCheckAll` is
// part of `verify`. The formatter's own version is pinned in .scalafmt.conf.
addSbtPlugin("org.scalameta" % "sbt-scalafmt" % "2.6.2")

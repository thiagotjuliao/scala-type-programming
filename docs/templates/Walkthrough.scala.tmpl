package typeprog.{{PACKAGE}}

/** Chapter {{NN}} — {{TITLE}}.
  *
  * Read this file top to bottom before opening the exercises. It is the whole
  * chapter in executable form: every claim below is one the compiler is
  * checking as you read it.
  *
  * Prose version: `docs/theory/ch{{NN}}-{{SLUG}}.md`.
  */
object Walkthrough:

  // TODO: the chapter, in runnable form. Delete the line below once there is
  // something here — an object with nothing but a comment in it does not parse,
  // so the template needs one member to stand on.
  private val nothingHereYet: Unit = ()

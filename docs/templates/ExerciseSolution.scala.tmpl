package typeprog.{{PACKAGE}}

/** Exercise {{EE}} — <!-- TODO: same headline as the stub -->. *Solved.*
  *
  * TODO: why this is the answer — the reasoning, not a restatement of the code.
  * A solution that only shows *what* compiles teaches nothing the reader could
  * not have got from `git diff`.
  */
object Exercise{{EE}}:

  type Answer = Unit
